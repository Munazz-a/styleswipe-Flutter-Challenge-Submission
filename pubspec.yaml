name: styleswipe
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: "^3.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.6
  provider: ^6.1.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  webview_flutter: ^4.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true